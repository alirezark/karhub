/*
 * JobSearch Messages
 *
 * This contains all the text for the JobSearch component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.JobSearch';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the JobSearch component!',
  },
});
