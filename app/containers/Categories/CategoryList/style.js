import { makeStyles } from '@material-ui/core';

const styles = makeStyles(theme => ({
  root: {
    marginTop: '10rem',
  },
  head: {
    margin: '4rem 0 3rem',
    fontWeight: 900,
  },
}));

export default styles;
