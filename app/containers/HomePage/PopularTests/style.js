import { makeStyles } from '@material-ui/core';

const styles = makeStyles(() => ({
  root: {
    marginTop: '5rem',
  },
  sectionHead: {
    fontWeight: 'bold',
    margin: '2rem 0',
  },
  subTitle: {
    marginBottom: '3rem',
  },
}));

export default styles;
