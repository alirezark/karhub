import { makeStyles } from '@material-ui/core';

const styles = makeStyles(() => ({
  root: {
    marginTop: '5rem',
  },
  sectionHead: {
    fontWeight: 'bold',
    margin: '2rem 0',
  },
  section_img: {
    float: 'right',
    marginRight: '2rem',
  },
}));

export default styles;
