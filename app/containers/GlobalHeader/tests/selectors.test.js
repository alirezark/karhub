// import { selectGlobalHeaderDomain } from '../selectors';

describe('selectGlobalHeaderDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
