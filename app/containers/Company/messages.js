/*
 * Company Messages
 *
 * This contains all the text for the Company container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Company';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Company container!',
  },
});
