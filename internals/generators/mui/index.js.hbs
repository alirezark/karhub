import React from 'react';
import { withStyles } from '@material-ui/core';
import {{ properCase name }} from '@material-ui/core/{{ properCase name }}';

const M{{ properCase name }} = withStyles(theme => ({
  root: {},
}))(props => <{{ properCase name }} {...props} />);

export default M{{ properCase name }};
